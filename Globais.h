#ifndef GLOBAIS_H
#define GLOBAIS_H


const float WIDTH = 800;
const float HEIGHT = 600;
const float GRAVITY = 0.25;

const float VEL_INICIAL = -3;

enum gameState {MENU, PLAY, LOST, DONE};
enum playerState {RUN, JUMP, FALL};
enum tela {MAIN, INFRM};

#endif // GLOBAIS_H
